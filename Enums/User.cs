namespace UserApi.Enums
{
    public enum ActiveMessage
    {
        BLOCKED,
        DELETED,
        ACTIVE
    }

    public enum AccessMessage
    {
        SUCCEED,
        PASSWORD,
        EMAIL,
        SERVICE
    }
}